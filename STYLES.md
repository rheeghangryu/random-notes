# 노트 작성용 스타일 문법

`notes/*.md` 파일을 쓸 때 그때그때 붙여 넣어 쓰는 태그 모음. app.js의 `loadNote()`가
읽어서 HTML로 바꿔준다. 새 스타일을 추가하려면 app.js의 태그 목록(`["source", "rheeghang"]`)에
이름을 더하고, style.css에 `#noteContent .이름` 규칙을 하나 추가하면 됨.

## 출처 표기 — `::source::`

인용문 아래 출처를 적을 때 사용. 보라색 포인트로 표시됨.

```
<br>
::source::
한병철, 『피로사회』 32p, 깊은 심심함
::/source::
```

## rheeghang 스타일 — `::rheeghang::`

본문이나 인용문 사이에 직접 끼어드는 개인적인 말/코멘트. 왼쪽 세로선 + 여백으로 구분됨.

```
::rheeghang::
(여기부터는 내 생각인데) 이 부분은 정말 공감이 됐다.
::/rheeghang::
```

## 각주 — `::footnotes::`

본문 중간에 닫는 태그(`::/footnotes::`)와 함께 쓰면 그 자리에서 바로 각주 스타일 블록으로 바뀜.
문서 맨 끝에서 닫는 태그 없이 한 번만 쓰면, 그 아래 전부를 각주 블록으로 처리함(001, 003, 009번 노트 참고).

```
::footnotes::

[1] 리강
설명...

::/footnotes::
```

## 윗첨자 — `^텍스트^`

캐럿(^)으로 감싼 부분이 `<sup>`로 바뀜. 페이지 표기나 각주 번호 등에 사용.

```
152^p^에서 이어지는 문장^[1]^
```

## 노트 간 링크 — `[[제목]]` / `[[id|보여줄 텍스트]]`

```
[[013]] 혹은 [[013|어디에나 보러 가기]]
```
